program_version = [print(text) for text in input().split() if len(text) % 2 == 0]


# program_version = [text for text in input().split() if len(text) % 2 == 0]
#
# for word  in program_version:
#     print(word)